export enum KeyDownEnum {
    LEFT = 'ArrowLeft',
    RIGHT = 'ArrowRight',
    UP = 'ArrowUp',
    DOWN = 'ArrowDown'
}

export enum MoveDirection {
    UP = 'upInterval',
    DOWN = 'downInterval',
    RIGHT = 'rightInterval',
    LEFT = 'leftInterval'
}